# Codex Handoff Pointer

The old long-session handoff has been compressed.

Use these current sources instead:

- `docs/CODEX_CLAUDE_COLLABORATION.md` for role split, backlog, review rules, Git rules, and token-saving rules.
- `build/reports/agent_handoff/latest_claude_packet.md` for the latest Claude/Codex local handoff.
- `docs/MODELING_WORKFLOW_DRAFT.md` for the current modeling workflow.
- `docs/MODELING_PATTERN_LIBRARY.md` for reusable lab-model patterns.
- `docs/OLD_CONTEXT_KEYWORDS.md` for compressed keywords from the old `.ctx/context.db`.

Do not resume from the deleted old `.ctx` database or from chat history unless the user explicitly asks for deep archaeology.
