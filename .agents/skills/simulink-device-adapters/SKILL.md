---
name: simulink-device-adapters
description: Use when creating, reviewing, or standardizing device templates and adapter contracts for Simulink power-electronics and converter-interfaced power-system models in simulink_agent_v1, including SG, DFIG, VSC, MMC, LCC, storage, loads, lines, transformers, mask parameter introspection, InitFcn self-containment, trace metadata, physical/signal port naming, and build-script adapter checks.
---

# Simulink Device Adapters

Use this skill when a model build copies, creates, or edits device templates or
adapter-facing subsystems.

Primary helper:

```matlab
cd("C:\Users\jonas\Desktop\simulink_agent_v1")
init_simulink_agent_project
addpath("scripts/adapters")
r = inspect_device_adapter_contract("nebus39_dfig2_weakgrid_v0", ...
    "ReportPath", "build/reports/adapters/nebus39_dfig2_weakgrid_v0.md");
assert(r.passed)
```

## Adapter Contract

Each reusable device template or copied donor subsystem should expose:

- stable block name (`DFIG_W33_a`, `G30`, `VSC_01`, etc.)
- explicit three-phase physical ports when it connects to SPS
- ordinary Simulink ports only for control/measurement signals
- mask parameters or InitFcn variables that can be introspected
- self-contained model InitFcn for donor workspace aliases such as `Ts`
- trace metadata in `UserData` when the device is generated by a build script

Read `references/adapter-contract.md` before changing build scripts or adding a
new device family.

## Routing

- Use `simulink-spec-validator` before choosing adapters.
- Use this skill in S2 build reviews.
- Use `simulink-model-verification` after adapters are instantiated.
- Use `power-electronics-tuning` for controller knobs after the device exists.
