interface:
  display_name: "Perturbation / Stability Boundary Scan"
  short_description: "Scan stability boundaries reproducibly"
  default_prompt: "Use $perturbation-stability-boundary-scan to run or plan deterministic stability-boundary scans with seeds, refinement, diagnostics, and artifacts."
