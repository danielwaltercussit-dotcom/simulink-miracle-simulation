interface:
  display_name: "Lab Model Pattern Miner"
  short_description: "Mine lab archive modeling patterns"
  default_prompt: "Use $lab-model-pattern-miner to scan the read-only lab archive for reusable model facts and report drift against the pattern library."
