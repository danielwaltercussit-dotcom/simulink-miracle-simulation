interface:
  display_name: "Baseline Regression"
  short_description: "Compare Simulink models to baselines"
  default_prompt: "Use $baseline-regression to compare a generated Simulink model with a golden baseline using explicit signals, tolerances, and artifacts."
